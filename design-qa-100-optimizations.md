# 鲁肃个人站双端 UI / UX 100 项优化清单

基线日期：2026-07-17  
覆盖范围：桌面 Neo-XP / Pixel Glass OS、手机虚拟 OS、首页、账号、知识库、视频、资源与临时互传、游戏、杂谈、关于、聊天室、日语训练器。  
原则：保持现有视觉识别、后端权限与数据模型；优先提高可读性、可达性、操作反馈、响应式稳定性和低性能设备流畅度。

## A. 全站基础与设计系统（1–10）

1. [x] 统一正文、辅助文字和禁用文字的最低对比度，减少浅蓝底上的低对比灰字。
2. [x] 为所有键盘操作控件提供一致且不被裁剪的 `:focus-visible` 焦点环。
3. [x] 把主要触控控件最低命中区统一到 44×44px，图标本体仍保持紧凑。
4. [x] 为长标题、文件名、标签和三语文案建立统一换行/省略策略，避免横向溢出。
5. [x] 统一按钮的默认、悬停、按下、禁用和忙碌状态，减少“点了没反应”的感觉。
6. [x] 为异步加载、空数据、失败和重试建立一致的状态组件节奏与可读文案。
7. [x] 收敛阴影、边框、圆角和玻璃透明度层级，保持 Neo-XP 风格但降低视觉噪声。
8. [x] 统一内容区最大阅读宽度与段落行高，避免桌面超长行和手机拥挤行。
9. [x] 完善 `prefers-reduced-motion`，关闭非必要位移、缩放和持续动画。
10. [x] 统一安全区、动态视口高度和底部 Dock/任务栏占位，避免内容被浏览器 UI 遮挡。

## B. 手机虚拟 OS 与导航（11–20）

11. [x] 移除重复的顶部时间与 `LUSU OS` 状态行，并收回占用高度。
12. [x] 压缩手机 Appbar 的无效留白，同时保留返回、标题和操作按钮的 44px 命中区。
13. [x] 为 Appbar 长标题增加单行省略和完整 `title`/无障碍名称。
14. [x] Dock 选中态在路由请求开始时即时响应，页面完成后再校准。
15. [x] Dock 横向滚动增加边缘提示与 `overscroll-behavior`，避免带动整页。
16. [x] 手机 Home 图标网格统一标签行高，三语长名称不推高相邻图标。
17. [x] 手机横屏减少顶部/底部装饰占比，让核心内容优先获得高度。
18. [x] 软键盘出现时仅滚动当前表单控件到可见区，不产生整页跳动。
19. [x] 返回手势/浏览器返回与站内返回保持同一路由状态和焦点恢复。
20. [x] 为手机 320–430px 宽、短屏和横屏建立明确回归守卫。

## C. 桌面壳、窗口与任务栏（21–30）

21. [x] 统一桌面窗口标题栏高度、图标尺寸和控制按钮命中区。
22. [x] 避免标题栏操作与长标题重叠，窗口标题允许安全截断。
23. [x] 窗口内容高度统一使用动态视口计算，减少双滚动条。
24. [x] 任务栏当前页选中态、悬停态和键盘焦点态清晰区分。
25. [x] 窗口切换保留轻量淡入，不让壁纸、顶栏和任务栏重复参与动画。
26. [x] 账号弹层、模态框和窗口建立稳定层级，避免被正文窗口遮挡。
27. [x] 桌面窗口在 1024px 附近减少无效外边距，提升内容利用率。
28. [x] 超宽屏限制窗口最大宽度与正文行长，避免信息过度拉伸。
29. [x] 短桌面视口压缩装饰性间距，保证主要 CTA 始终可达。
30. [x] 统一可拖/不可拖区域光标和文本选择行为，降低误操作。

## D. 首页与账号流程（31–40）

31. [x] 首页首屏强化“进入各模块”的视觉优先级，降低次要装饰竞争。
32. [x] 最近更新卡片优化标题、日期、摘要和 CTA 的纵向节奏。
33. [x] 首页图标提供一致的悬停、按下与键盘激活反馈。
34. [x] 语言切换显示当前语言并提供明确的可访问名称。
35. [x] 登录/注册模式使用可见标题和按钮状态区分，避免提交意图混淆。
36. [x] 账号表单错误定位到具体字段，并用 `aria-live` 汇总反馈。
37. [x] 登录、注册、退出请求期间防重复提交并显示忙碌状态。
38. [x] 账号弹层打开后聚焦首个有效控件，关闭后恢复原触发器焦点。
39. [x] 手机非 Home 模块也能直接进入账号操作，不依赖隐藏顶栏代理点击。
40. [x] 首页时间段壁纸装饰在低性能/省流量环境下降级为静态层。

## E. 知识库与文章阅读（41–50）

41. [x] 知识库筛选按钮显示选中态、结果数量和无结果反馈。
42. [x] 文章卡片统一分类、日期、标签、标题与摘要间距。
43. [x] 标签过多时换行并限制视觉密度，不遮挡发布时间。
44. [x] 文章正文限制舒适行宽，提高中英日三语段落行高。
45. [x] 标题层级、列表、引用、代码与链接建立一致阅读样式。
46. [x] 手机文章去掉与进度条重复的栏目文字和百分比文字。
47. [x] 阅读进度条不遮挡返回、复制、回顶和正文内容。
48. [x] 复制文章链接提供成功/失败反馈，且失败时允许手动复制。
49. [x] 回到顶部按钮在固定 Appbar 下保持可点击并恢复阅读焦点。
50. [x] 深链接加载、文章不存在和网络失败提供稳定返回路径。

## F. 视频区与播放器（51–60）

51. [x] 视频卡片封面统一 16:9，使用 `object-fit` 防止拉伸。
52. [x] 封面缺失时使用现有品牌内的真实兜底资源与可读标题。
53. [x] 视频筛选与知识库筛选共享一致的选中、数量和空态反馈。
54. [x] 视频标题、来源、日期和简介在卡片内保持对齐。
55. [x] 播放弹窗打开时聚焦关闭控件，关闭后恢复原视频卡片焦点。
56. [x] 播放器加载中显示稳定占位，避免 iframe 载入引发布局跳动。
57. [x] 外部播放链接明确标注新窗口行为并保留 `noopener`。
58. [x] 手机播放器使用可用视口和安全区，不被 Dock/浏览器栏遮挡。
59. [x] 视频错误时提供重试和打开原地址两个互不冲突的动作。
60. [x] 弹窗关闭时停止媒体，避免后台继续播放或占用带宽。

## G. 资源区与临时互传（61–70）

61. [x] 资源卡片统一高度节奏、元信息换行与 CTA 对齐。
62. [x] 临时互传从相册、文件、拖放和粘贴添加的附件先进入待发送托盘。
63. [x] 手机提供独立相册入口且不强制相机，同时保留多文件入口。
64. [x] 待发送图片使用小缩略图，文件使用紧凑图标卡片并可单独移除。
65. [x] 只有再次点击发送才创建上传任务，文字失败时保留附件可重试。
66. [x] 发送期间锁定重复提交、选择、拖放和移除，避免批次竞态。
67. [x] 上传任务绑定创建时的房间上下文，离房时取消，杜绝跨房发送。
68. [x] 已发送图片限制在聊天卡片内，普通文件显示类型、名称、大小与 MIME。
69. [x] 每个附件提供下载按钮，每条解密文字末尾提供复制按钮。
70. [x] 上传进度、暂停、恢复、取消、失败和配额文案保持三语一致且可读。

## H. 游戏、杂谈与关于（71–80）

71. [x] 游戏卡片统一封面比例、标题基线、描述高度和启动按钮位置。
72. [x] 游戏加载失败区分资源缺失、网络错误与浏览器不支持，并提供重试。
73. [x] 进入游戏前保留来源、控制方式和存档说明，减少误解。
74. [x] 游戏 iframe/画布获得明确标题，并在退出时恢复卡片焦点。
75. [x] 杂谈卡片统一日期、标题、摘要和正文展开节奏。
76. [x] 杂谈空态不显示伪占位内容，提供返回首页或其他模块入口。
77. [x] 关于页简介控制行宽，长段落拆分为可扫读的信息组。
78. [x] 社交图标提供可见悬停/焦点状态和准确平台名称。
79. [x] 隐藏未配置的社交入口，不展示无效链接或占位按钮。
80. [x] 所有外部社交链接明确新窗口行为并使用安全 `rel` 属性。

## I. 聊天室（81–90）

81. [x] 聊天消息正文保持纯文本安全渲染并改善长词/URL 换行。
82. [x] 消息列表使用单一滚动容器，输入区在手机软键盘下持续可达。
83. [x] 发送中禁止重复提交，成功后清空，失败时保留草稿。
84. [x] 冷却时间、离线、同步中和失败状态使用一致的状态位置。
85. [x] 新消息到达时仅在用户接近底部时自动滚动，阅读历史时不抢位置。
86. [x] 未读消息提供轻量提示和返回最新消息动作。
87. [x] 密码房与普通大厅用清晰标题/说明区分，不依赖颜色单独表达。
88. [x] 密码输入和房间切换避免泄露内容，退出后清理本地敏感状态。
89. [x] 聊天输入框提供字符边界与发送快捷键说明，手机仍保留明确发送按钮。
90. [x] 空房间、加载失败、被限流和会话失效都提供下一步操作。

## J. 日语训练器、性能与质量门槛（91–100）

91. [x] 日语训练器桌面与手机顶栏、返回、存档状态保持同一信息优先级。
92. [x] 题目、选项、解析和重答动作在短屏上不形成操作死路。
93. [x] 播放/暂停、进度和倍速控件提供一致焦点态与足够命中区。
94. [x] 音频切换时取消旧请求/旧高亮，离开关卡立即停止播放。
95. [x] 月历、连续天数和云同步状态在三语下保持紧凑可读。
96. [x] 图片、视频封面和非首屏资源按尺寸设置懒加载与异步解码。
97. [x] 为关键媒体预留宽高比，减少 CLS 和滚动跳动。
98. [x] 对高成本阴影、模糊和动画提供移动/低性能降级。
99. [x] 新增桌面、手机竖屏、短屏、横屏与 reduced-motion 的静态回归守卫。
100. [x] 建立发布验收表：三语、键盘、触控、空态、错误态、缓存 query、CI 与 Preview 逐项确认。

## 执行记录

`[x]` 表示该项已经通过现状审计并有回归守卫，或已在本分支补齐实现；不表示 100 项都重写了代码。已有良好行为保留并验证，部分/缺失项才做针对性改动。

- 全站与双端壳：补统一焦点环、粗指针 44px 命中区、辅助文字对比、长文 78ch、非 Home 账号入口、Appbar 完整标题、Dock 边缘提示、软键盘定向滚动、焦点记忆和低性能降级。
- 账号/文章/视频：账号改为显式登录/注册模式和单一主按钮，字段错误可定位；文章复制失败显示可选 URL，回顶恢复标题焦点；播放器增加稳定载入层、错误重试和新窗口原地址动作。
- 临时互传：保持附件先暂存再发送，并补 20 项/500 MiB 批次保护、焦点恢复、内部发送锁、三语进度/ETA、图片比例预留和安全下载参数。
- 游戏/日语工具：游戏错误分类、重试、控制与存档说明、返回焦点；日语工具顶栏层级、漏答聚焦、音频 ARIA、旧高亮清理、320px 月历和媒体尺寸均已补齐。
- 聊天：发送绑定不可变房间上下文，私密/公开草稿仅在内存按房间隔离；历史阅读时不抢滚动，提供未读/回到最新、离线/重试和 Ctrl/Command + Enter。
- 质量门槛：新增核心 UI、游戏、互传和日语专项合约测试；现有 GitHub Verify 继续守卫 PR / `main`，Preview 部署状态在独立验收表记录；本轮修改的公共资源 query 统一为 `20260717-100-ui-ux-preview-r1`。

分支只用于 GitHub / Cloudflare Preview，不合并 `main`，直到站长完成验收。Preview 不执行文件上传或生产 D1 写入测试；R2 Preview 仍按仓库约定显式关闭。
