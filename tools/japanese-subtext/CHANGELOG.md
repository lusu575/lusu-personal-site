# 日本語の裏側 更新记录

本文件记录“日本語の裏側”的公开版本历史及当前待发布版本的阶段状态。记录一经发布即保持原文；后续版本必须新增条目，不覆盖或回写旧条目。待发布条目必须明确标注，只有真实上线后才转为不可回写的公开历史。网站知识库中的上线记录与版本更新也遵守同一规则：一次上线或一次版本发布，对应一篇独立的三语文章。

## WIP 归档 — 2026-07-16

- 将应用 `1.0.4` / 内容 `1.0.3` 候选冻结到独立 WIP 分支，保留代码、题库、10,088 个 MP3、250 份时间轴、Image2 canonical jobs、prompt、设计身份与生产管线；本次归档不合并 `main`、不部署，也不改变当前公开版本。
- 补存 48 份清理前工作区中的 `codex-approved` Image2 review；连同原有记录，仓库现有 62 份 review。每份证据只绑定对应旧 PNG 的 toolRunId 与 SHA-256，不能沿用到以后生成的新图。
- 保留 14 张未引用 checkpoint WebP 作为溯源参考；252 份 current-v4 完整图片资产仍缺 238 份。旧 checkpoint／review 不作为正式资产自动导入或发布，下一轮默认重新生成完整图片包并重新审核。
- 归档校验中，10,088 个音频／250 关快速验证通过，62 份 review 的状态、六项检查与唯一证据绑定通过；Node 测试为 131／146 通过，其余用例受缺失 `sharp` 与 Windows CRLF／LF prompt 差异阻断。主站构建继续按设计拒绝不完整 Image2 与 `PENDING` 浏览器门禁。
- 新增 [`WIP_STATUS.md`](./WIP_STATUS.md)，集中记录已保存内容、明确缺口、恢复点和后续发布前门禁。

## 1.0.4 — 待发布（内容 1.0.3；音频优先候选：2026-07-14）

- 本轮逐图生产暂停时，45 个候选只有 13 个形成 prompt/review/source/raw/sidecar 一致链。恢复阶段重新核验中断事务中的 L1-003 原始工具产物、归一化 PNG、review、sidecar 与当前 prompt/style/source hash，确定性重建 WebP 后将未引用 checkpoint 恢复到 14 张；250 张关卡图加两张背景仍缺 238 张。
- 音频优先候选通过显式 `--allow-legacy-illustrations` 把锁定题库构建为内容 `1.0.3`；当前运行时仍使用 250 张 `assetContentVersion: 1.0.2` legacy illustrations，并明确标记 `transitional-audio-first`。该过渡没有放宽默认构建、image2 检查或正式 release gate。
- 新增受同一 OS 发布锁保护的 `jp-subtext:audio:rebind`：`prepare` 固化 manifest、10,088 个 MP3、250 份时间轴及稳定 audio／scene source 投影，`rebind` 只允许内容版本和可证明安全的来源绑定变化，`check` 复核前后 journal、集合哈希和逐关证明；任何 reading、voice、line／token／option、停顿、cue 或媒体字节变化都会拒绝离线改绑。
- 音频 manifest 已从内容 `1.0.2` rebound 到 `1.0.3`，覆盖 10,088 个 MP3、250 份时间轴，总长 `42,533.531` 秒、总计 `518,739,675` 字节。rebind 期间 MP3 工作树字节改动为 0；manifest 与 timeline 只更新内容绑定及其证明。
- 待发布音频管线继续使用本机 AivisSpeech Engine 1.2.0 和四套许可清晰的 AIVMX 日语模型在 CPU 上生成；显示汉字与纯假名合成输入分离，reading / mora / query / 模型来源和清晰度证据全部进入 manifest。audio rebind 已完成不代表应用发布，仍须随完整候选执行正式 release check。
- 全库可朗读任务通过 reviewed reading 复核与回归锁定人名、复合词、计数词、外来语和可朗读场景指令；“今日”统一锁定为 `きょう`，发音覆盖必须真正体现在最终 reviewed reading。最终音频差异只在增量生成和全量验证完成后统计。
- 速率调整升级为 `post-synthesis-active-mora-rate-v3`：以 6.5 mora/s 为目标、校准样本不高于 6.6 mora/s，同时继续以最终 MP3 不高于 7.2 mora/s 作为硬门禁；全部非场景 artifact 与发布条目绑定完整策略证据，策略升级会使旧版未调速音频也失效重录。
- 单 mora 自然迟疑「ん……」使用专用 `hesitation` 速率带和 `1.2 mora/s` 下限；该例外严格绑定审校假名，普通短词仍以 `1.5 mora/s` 为下限，并由生成器、媒体审计与构建守卫交叉验证。
- 全量生成加入同卷关卡媒体快照、manifest 原子回滚与可直接重试的 failure report；单关失败会恢复 MP3/scene/timeline 后继续后续关卡，但整批仍以非零状态明确拒绝发布。
- 250 张关卡图与桌面／手机两张应用外背景固定由 `gpt-image-2` 按任务逐张生成；禁止 CSS、SVG、程序化图和本地 fallback。canonical Image2 v4 jobs 已重建并通过来源、身份、生成／导入、迁移和发布管线契约测试，但 252 张原图的逐图生成、Codex 审核、导入与发布尚未完成。重复生成的场景拓扑或角色身份错误只允许用受测的 source-preserving addendum 修正；L1-017 禁止把安静世界画成决定性目的地，L1-048 保持机器人身份，L1-049 禁止选择/便笺映射答案，L2-005 保持群聊独立端点并禁止复现笑话，L2-012 的资料页禁止伪文字/表格排版。
- Codex 内置 image2 视觉审核的证据状态改为 `codex-approved`，保留每份记录原有的 Codex reviewer 标识、六项显式视觉检查、toolRunId 与原始 PNG SHA-256 绑定；不再将 Codex 审核声称为 `human-approved`，旧状态由导入和发布门禁拒绝。
- v4 设计身份注册表现有 780 个 cast reference、778 个 design identity，只允许 `person:l4-hori`（L4-008／L4-014 的 Hori 同一人物）与 `appearance:l3-036-yui`（L3-036 的 Yui／Predictive Avatar 同外观）共享核心设计。L5-043 的未识别咳嗽声 `cough` 使用独立 `stage:l5-043:cast:cough` identity，不与人物、Avatar 或非人系统共享。
- L5-001～L5-050 已完成内容、三语解释、证据与 reviewed reading 审计；L5-011～L5-020 的关键读音／证据回归已补入测试。
- 远程／网络场景的 prompt 额外要求四个主格各自保持单一未分割镜头，并在不同地点之间以整格交替，拒绝会把四格扩成更多视觉分格的斜线分屏、内部边框与画中画。
- 对连续生成出远程人物共处、聊天气泡或结论性手势的关卡加入可复现的题面约束 addendum；只重算对应 promptHash，禁止用未入库的临时提示词修图。
- Image2 任务新增按 stageId 稳定注入且自动去重的文字证据、非人系统与高密度四格共享约束，并以逐关受测 addendum 处理字幕、选项、时序、身份与证据冲突；addendum 只进入最终 prompt 与 `promptHash`，不进入 `sourceTextHash`。
- L2-013、L2-016、L2-018、L2-019 与 L2-020 补入 source-preserving Image2 约束：保留汤/盐罐、VR 默认头像/登录通知、货船 AI、会议室实物证据与猫咖零食袋等题面锚点，同时禁止罪恶反应、Yuma 实体化、氧气余量图形、未知访客剪影与猫的有序路径直接泄题；重生 canonical prompts 后仅这 5 关 `promptHash` 变化，250/250 `sourceTextHash` 不变。
- L2-013 的 Image2 约束再次收紧：盐罐只能作为调理台背景中唯一、微小、无标签的普通容器，四格内三人始终互相面对并保持克制的试吃交流；禁止人物看向、转向、指向、触碰盐罐，禁止特写/高亮/构图引导，也禁止夹板、笔记本、菜单、评分表等题面外服务道具。重建 canonical prompts 后仅该关 `promptHash` 更新，`sourceTextHash` 与其余 249 关保持不变。
- L2-021～L2-028 补入逐关 source-preserving Image2 约束：分别保留广播室雨伞/空白名牌、三端工作聊天、美术馆入口相机、三端语音聊天/空白系统日志、高价餐厅点单、便利店工作服/咖啡/电池/空白小票、远程留守电与雨中采访等题面锚点；禁止所有权核验、责任归属、禁拍结论、音量修复、价格/报销、钥匙位置和未来丰收等答案链，以及人物共处、无关道具和提前结局。重建 canonical prompts 后仅这 8 关 `promptHash` 变化，250/250 `sourceTextHash` 不变，选项、正确答案、证据 ID 与解析原文精确泄漏均为 0。
- L2-031～L2-040 补入逐关 source-preserving Image2 约束：保留实体审查室、同一份待确认修订、四端群聊、遗失物柜台、事后餐厅询问、非人支援 AI、停电居住区与分离播报端、魔法城门空白钟面、城堡厨房及酒店客房/前台电话拓扑；禁止批准/怒意结论、出行承诺、雨伞核验、用餐重演、权限操作路径、停电时序、讽刺结论、果酱目的与自动电话来源等答案链，以及题外人物、道具和完成态。重建 canonical prompts 后仅这 10 关 `promptHash` 变化，250/250 `sourceTextHash` 不变，选项、正确答案、证据 ID 与解析原文精确泄漏均为 0。
- L2-041～L2-047 与 L2-050 补入逐关 source-preserving Image2 约束：保留分离的月面新闻端点、同一虚拟会场头像、内部纯暗的打开宝箱、三端家庭群聊、人工给茶机器人与等权空白席札/饮料、植物照料语音两端、实体产品发布会议和深夜电台两端；禁止月面通勤运营、多选线索高亮、服装规则映射、钥匙/奖励、标点/情绪结论、席札身份映射、返程日期、法务矛盾结论及昵称重连等答案链和题外元素。重建 canonical prompts 后仅这 8 关 `promptHash` 变化，L2-048/049 保持原样，250/250 `sourceTextHash` 不变，选项、正确答案、证据 ID 与解析原文精确泄漏均为 0。
- 新增 Codex 内置 `image_gen.imagegen` 单图导入链：项目外原始 PNG 经无裁切 Lanczos3 归一化，持久 Codex 六项视觉审核记录与 toolRunId/源图 SHA 一起绑定进 sidecar；发布器严格区分内置工具证据与 Images API request 证据，禁止混写或伪造 request ID。
- 1.0.3 Image2 manifest 生效后，构建守卫拒绝 `assets/stages/` 顶层遗留的旧 `manifest.json` 和 `l[1-5]-NNN.webp`，避免新旧配图重复发布与占用空间。
- 收紧桌面与手机关卡布局，完善首次模式选择、声音失败回退、答题结果/解析/下一关流程和打卡式云存档；手机端关键状态/提示文字保持 16px 可读基线，模式弹窗被阻止关闭时只聚焦仍可用的入口；满量云同步固定为 8 条 D1 查询并保持原子回滚。
- 结果弹窗禁止用 Esc、遮罩或关闭叉绕过后续动作，避免提交后落入不可操作状态；选项使用固定 ✓/× 图形配合红绿反馈且不追加会撑宽的答案文字，失败媒体的“重新加载”按原 line/token/option context 精确重试。
- 公开页面、嵌套模块和主站入口的缓存键同步到 `20260712-japanese-subtext-v103-r6`；主站入口保留当前界面语言，手机竖版背景只在 900px 内竖屏使用，横屏继续使用桌面横版背景。当前发布报告的 AUDIO / IMAGE2 / BROWSER 三道门继续以新合同保持 PENDING。
- 上线、1.0.1 与候选 1.0.3 的三语文章补齐不可回写 SHA-256 指纹，和 1.0.2 一起阻止同步修改三份 seed 后绕过历史记录守卫；候选 1.0.3 文章只在版本门槛完成并部署后成为公开记录。
- L3-001～L3-010 增加逐关 source-preserving Image2 addendum，锁定实体/远程/虚拟/非人系统拓扑并禁止用资金、评分、时间、选中消息、战斗、同步、假设、无人机和档案证据链觉泄题；L3-009/010 的普通日志审查不再误判为远程通信。prompt 审计会把题面本身已出现的 `船長`、`回収用ドローン` 归类为 canonical source collision，同时继续拒绝真正新增的选项、正确答案与解析文本。重建后仅这 10 关 `promptHash` 变化，250 个 `sourceTextHash` 全部保持不变。
- L3-011～L3-015 增加逐关 source-preserving Image2 addendum，保留同一委员会/提案会/餐桌/酒店前台及分离聊天端点，禁止责任、采用、代付、补偿与已读时序答案链被画面提前解出；L3-013 的“通信量”已设为题面确认的本地语境。canonical 重建仅改变这 5 关 `promptHash`，250 个 `sourceTextHash` 保持不变，真实选项/解析泄漏与 source collision 均为 0。
- 第二轮直接成图复审撤回 L1-007、L1-010、L1-018、L1-019：分别禁止把离线后画成仍在交互、把待出炉面包画成已端上、用汗滴/抓头直接表现说谎，以及把无人 kiosk 画成真人客服。四关 source-preserving addendum 和 canonical prompt 已受测重建，题库/source hash 不变，旧图不得进入 1.0.3 发布。
- 成图二次复审追加撤回 L1-029/030/031/032/035/036：修正非人游戏系统、深海考试逐步显现、文档/投影/售货机伪文字与远程手机叠层问题。六关新 prompt 已受测重建，旧 raw 会因 `promptHash` 过期被发布门拒绝。
- L1-046/047/049/050 撤回并重建提示约束：保持杯子被猫两次推动的连续位置，借书不在留言两端复制，清除游戏开发屏幕/便笺伪文字，并锁定腕表、两杯与对面空座的题面动作；L1-048 保持 PASS。
- L3-016～L3-020 锁定留言两端、广播制作环境、校内扬声器、非视觉旁白与梦中车站，修正 L3-018 远程误判，并禁止用碎片匹配、利益链、火灾撤离、循环时间线和右侧箭头视觉泄题。
- L3-021～L3-030 增加逐关 source-preserving Image2 addendum：锁定占卜馆信封、谒见厅空白账簿、和平条约展柜、本地歌唱 AI 诊断、闭馆图书馆、电梯案内板、室内询问、事后事故听取、同一森林任务空间与姐弟书桌等题面锚点，禁止把门/钥匙比喻、债务/治理多选、条约文书、情绪波形、自动回收因果、楼层版面位置、湿伞/鞋底/出入记录、新闻记忆来源、任务改写和答案纸用途画成答案链。L3-024 的 diagnostic log、L3-027 的 access log 与 L3-029 的 upper stream 已固定为本地语境；L3-025 的蓝色发光札在黑白合同中只保留微弱灰阶光，L3-030 停在背面成为画纸之前并禁止“翻面→空白背面→宇宙船作画”完整链。canonical 重建只改变这 10 关 `promptHash`，250/250 `sourceTextHash` 不变，真实选项/解析泄漏与 canonical source collision 均为 0。`hero` / `reporter` 的既有 cast-id 别名仅记录为锁定题库数据风险，本批保留当前 ID、双语姓名与稳定设计，不扩张为全库重命名。
- L2 成图二次复审继续撤回 L2-001/002/003/006/007/008/010/011/012：墙面、资料、申请表、书脊与提案材料只保留无字大块等权几何；蛋糕严格锁定“上桌→品尝→克制回应/要奶油→第二口”，申请表不被凉接走且末格离开，咖啡按“拒绝惯常准备→改选同等无标签咖啡→询问→回答”展开；同时收紧三人表情、远程端点姿态、空座/最小餐桌、大包完整包体与双把手、悬停未抓取、未使用无印迹印章，并删除肩包、平板、圆点标签框和题外装饰。九关受测 canonical prompt 已重建，仅这 9 关 `promptHash` 变化，250/250 `sourceTextHash` 不变，真实选项/解析泄漏与 canonical source collision 均为 0；不改题库、raw、review 或成图。
- 截至 2026-07-14，canonical v4 已按全库最新题库与 addenda 重建并通过契约测试，但有效 Image2 checkpoint 仍为 14／252、缺口 238；release check、五视口三语浏览器回归、GitHub main 合并／推送和 Cloudflare Pages 部署仍未完成，`1.0.4` 只能保持未发布的音频优先候选状态。
- 三语更新文章已从错误的“图片已完成／版本已发布”改为可更新的音频优先候选进度，明确 10,088 音频已验证、250 张 legacy 图仍在过渡使用、Image2 仍缺 238 份且尚未上线；主站 `main.js` 缓存键同步升至 `20260714-japanese-subtext-v104-r2`。
- 最终音频全量 ffprobe／静音检查与 rebound 证据复核通过，发布报告 AUDIO 门禁已绑定实际 manifest／stats 转为 `PASS`；Image2 与 BROWSER 仍为 `PENDING`。

## 1.0.3 — 2026-07-14

- 修复错答后关闭结果弹窗、点击弹窗外或查看解析时失去重新答题入口的死路；题面和解析正文之前都保留重答入口，结果弹窗不能由关闭按钮、Escape 或外侧点击绕过。
- 下一关只按本次 `attemptCleared` 判断，历史已通关后本次答错仍进入重答流程。
- 正式拆分 `appVersion` 与 `contentVersion`：本次公开应用为 1.0.3，题库、10,088 件静态音频、时间轴、云进度与存档兼容继续保持内容 1.0.2。

## 1.0.2 — 2026-07-11

- 重建离线日语语音生成链，补充可审校假名、音素审计、实体校验与孤儿文件检查。
- 桌面端采用更紧凑的工具壳布局，解析页增加下一关入口。
- 学习记录改为月历打卡，并加入独立云端日活动同步。
- 关卡图改为统一的本地黑白四格分镜；该批资源属于代码生成 fallback，并非 image2 成品。

## 1.0.1 — 2026-07-11

- 完成三语标题、挑战入口和入口标签整理。
- 精简播放器，取消自动播放和重复控制，保留文字点击播放与进度定位。
- 增加首次模式选择、奖牌结果弹窗和假名优先语音流程。
- 修复中文字体、内部台词编号外显和播放强制滚动。

## 1.0.0 — 2026-07-11

- 首次上线 5 个难度、250 关的日语潜台词训练工具。
- 提供听力、日语、双语模式，逐句、词块与选项播放，以及本地/云端进度。
- 使用离线预生成音频，浏览器运行时不加载 TTS 模型。
