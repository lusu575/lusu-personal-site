export const REMOTE_ORIGIN = Object.freeze({ source: "whiteboard-remote" });
export const LOCAL_SCENE_ORIGIN = Object.freeze({ source: "whiteboard-local-scene" });
export const LOCAL_ASSET_ORIGIN = Object.freeze({ source: "whiteboard-local-asset" });
