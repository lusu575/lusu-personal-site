# 在线画板 CHANGELOG

本日志只记录在线画板子项目。根项目发布历史仍写入仓库根 `CHANGELOG.md`。

## 1.0.8 - 2026-08-20

- 使用 Image2 生成并保留来源清单的像素画大厅背景，以暖纸、XP 蓝和 Y2K 像素元素统一主站视觉；WebP 为首选资源，PNG 作为兼容回退。
- 重排大厅标题、身份、公共画板、密码房和最近使用卡片，统一边框、间距、操作落点与移动端单列布局。
- 密码房标题旁新增“？”说明：桌面悬浮或聚焦、移动端点击均可查看；中英日文案说明同密码入同房、密码需单独分享，以及最后参与者退出 24 小时后的自动删除边界。
- 本次仅修改大厅视觉与说明，不改变密码归一化、房间映射、WebSocket/Yjs 协作、匿名身份或数据保留逻辑。

## 1.0.7 - 2026-08-06

- 修复授权生产闭环发现的第二个 Pages 主 mutation gate 漏项：精确的 `POST /api/whiteboard/agent/scene` 且 `Content-Type: application/vnd.yjs-update` 的请求现在可跳过 JSON 内容类型门禁，继续进入既有 Agent Bearer、`whiteboard:write`、tokenId 绑定房间令牌、operation ID、正文上限与 Durable Object 只追加场景校验。
- 同源检查仍在媒体类型例外之前执行；跨源请求、相邻路径、非 POST 与其他 MIME 继续分别失败关闭。1.0.6 的 PNG／JPEG／WebP 精确上传例外保持不变，没有改成前缀、通配路径或宽泛媒体类型。
- 本次只修复在线画板的受管 Pages 入口；Quick Transfer 业务与受管路径均未改变，因此继续保持 1.0.6。

## 1.0.6 - 2026-08-06

- 修复生产点检发现的 Pages 主 mutation gate 漏项：`POST /api/whiteboard/agent/assets` 的 PNG／JPEG／WebP 现在和浏览器图片入口一样，在通过全局同源边界后继续进入 Agent Bearer、`whiteboard:write + whiteboard:assets` 与 tokenId 绑定房间令牌鉴权。
- 豁免仍按方法、完整路径与 MIME 精确匹配；跨源请求、相邻路径、非 POST、SVG／HTML／JSON 和其他内容类型继续失败关闭。Pages 与 Durable Object 的 5 MiB 限制、严格图片容器校验、私有 R2、幂等收据和只追加场景协议均未改变。
- 本次只修复在线画板的受管 Pages 入口；Quick Transfer 业务与受管路径均未改变，因此继续保持 1.0.6。

## 1.0.5 - 2026-08-06

- 新增受管 Agent 图片通道：上传同时要求非默认 `whiteboard:write` 与 `whiteboard:assets`，原图下载要求 `whiteboard:assets` 加场景读取权限；Agent Bearer、绑定当前 tokenId 的房间令牌和 Pages→DO 内部授权继续分层。
- CLI／stdio MCP 可上传、下载并用高层 `image` 元素把当前房已验证的 PNG／JPEG／WebP 放入画板。输入限制为常规文件、5 MiB、严格容器边界／关键块段／声明尺寸／像素上限；该边界不宣称完整像素解码。MCP 受 allow-root 与 realpath 约束，下载默认不覆盖，本机路径、令牌和内部房间 ID 不进入工具输出。
- 场景验证仍只允许追加：普通 write-only 客户端继续不能写图片；图片只能引用当前 DO 的权威 `ImageMeta`，资源记录必须规范且被本次图片使用，可复用既有记录或多次放置同图。既有元素／资源的改删、孤立资源、URL／Base64／SVG／HTML、跨房引用、链接、绑定、`customData`、未知根和任意 Yjs 注入继续拒绝。
- 图片上传以主体、operation ID 和字节 SHA-256 提供独立幂等收据；同字节重试返回同一资源，不同字节复用 ID 冲突。资源仍存私有 R2，并沿用每图、每房容量、未引用清理和密码房 24 小时生命周期；简化 SVG／PNG Agent 导出仍不嵌入图片并明确告警。
- 把白板专属 adapter、scene builder 与能力测试加入子项目 tracked paths，后续图片或高层场景协议变化不能因位于共享能力目录而绕过白板精确升版。

## 1.0.4 - 2026-08-06

- 将在线画板加入三项真实工具的固定机器可读目录契约，锁定 `whiteboard` 能力域与同源 `/tools/whiteboard/` 入口，目录不再依赖可变或任意启动目标。
- 三项工具契约按工具拆分；白板只追踪自己的专属模块。共享工具目录使用白板 `toolId` 锚点、有界窗口与精确版本模板校验，其他卡片的相同版本号不能掩盖白板卡片漏改；无关工具变化也不会误触白板升版。
- 本次只扩展本地 CLI／stdio MCP 的公开目录发现并同步可见版本；不改变房间口令、Agent scope、Yjs 场景协议、Durable Object、R2、生命周期或远程 MCP 部署状态。

## 1.0.3 - 2026-08-06

- 新增受 scope 约束的站点 Agent 房间加入与场景通道，Agent Bearer 和房间访问令牌分离；`whiteboard:read`／`whiteboard:write` 均为非默认权限，写权限只隐含读取，不扩大其他能力。
- Durable Object 只接受基于最新完整 Yjs 状态生成的追加式更新：单次最多新增 50 个受支持的文字／基础图形／线条／箭头，禁止修改或删除既有元素、图片、嵌入、链接、绑定及未知根数据。
- Agent 写入使用有界 `operationId` 与载荷 SHA-256 幂等收据，同一操作重试安全返回，换载荷复用 ID 则拒绝；收据与文档版本原子落盘，房间锁定时拒绝写入。
- 本地 CLI／stdio MCP 可安全加入房间、读取摘要、追加高层元素并导出 JSON／SVG／PNG；密码只能从标准输入或环境变量引用读取，不进入参数、句柄或响应。简化视觉导出不嵌入房间图片，并明确返回警告。
- 本地 `whiteboards.json` 使用跨进程 owner-token 锁保护整次 read-modify-write，并由同目录 0600 临时文件 fsync 后原子替换；并发加入／令牌刷新不再互相覆盖，写入失败也不截断上一份有效状态。
- 公网远程 MCP Worker 继续保持未部署、只读且不含画板写工具；本次专用 Agent HTTP 通道不是标准 OAuth 公网写入口。

## 1.0.2 - 2026-08-01

- 将公共画板和全部密码房显式锁定为同一套暖纸、石墨、hachure、高 roughness 铅笔草图默认值，不新增第二套房型主题。
- 可见页面改用每 60 秒一次的 WebSocket 静态 `ping/pong`；回复由 Durable Object auto-response 完成，不唤醒已休眠实例。隐藏标签页 60 秒后先排空未确认更新再停放连接，重新可见时自动差异同步。
- 客户端文档批处理调整为 250／500／1000ms，游标调整为 100ms；画布无变化时不发送 Yjs 持久化更新，连续活动时 D1 房间摘要最多约每分钟同步一次。
- 有连接房间的异常连接巡检从每 15 秒改为每 5 分钟，并使用最近 auto-response 判断 7 分钟失联；空公共房不再维持周期生命周期 Alarm，密码房仍只在最后离开后保留 24 小时删除计划。
- 普通连接波动不再弹出画布中央通用错误；不足 3 秒的恢复保持无提示，持续波动只显示角落小状态，权限、协议、容量和文件错误仍明确显示。

## 1.0.1 - 2026-08-01

- 修复快速绘制超过 Worker 每秒更新阈值后被 `1008 / rate_limited` 断开、未确认 Yjs 更新不再补传而导致线条退出后消失的问题。
- 客户端现在合并连续更新、按文档规模采用 Worker 建议间隔、一次只发送一个待确认批次；Worker 落盘后返回 `update-accepted`，断线或 ACK 丢失会把原批次安全重入队。
- 退出按钮会短暂等待待确认更新排空；限速关闭改为可恢复重连，并保留具体三语错误类型。
- 新增公共房跨断线/DO 唤醒永久保留回归，继续锁定密码房最后离开后 24 小时整房删除与重入取消删除。
- 默认新建内容调整为暖纸底、石墨灰、hachure、高 roughness 的铅笔草图风。
- 工具区卡片同步显示 `v1.0.1` 与本次更新日期，布局审计改为校验画板、临时互传和日语工具三张真实卡片。
- 建立独立版本、项目清单、AGENT/AGENTS、更新日志和文档联动门禁。

## 1.0.0 - 2026-07-30

- 初次提供公共房、密码房、Excalidraw + Yjs 实时协作、匿名身份、图片、导出、Durable Object 持久化与私有 R2 资源。
- 公共房不设空房 TTL；密码房最后离开 24 小时后自动清理。
