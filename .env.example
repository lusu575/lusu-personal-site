# For local development, copy this file to .dev.vars and fill both values.
# In GPTWork, configure both names as process-environment Secrets and do not create .dev.vars.
# Use two independent cryptographically random values of at least 32 UTF-8 bytes.
# Never commit real values.
CHAT_IP_HASH_SALT=
ANALYTICS_IP_HASH_SALT=
