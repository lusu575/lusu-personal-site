# 鲁肃个人站管理后台专用 CHANGELOG

> 管理后台专用说明：本文档只记录 `/admin/` 管理后台的私有维护记录，不等同于主站根目录 `CHANGELOG.md`。后台私有更新不写入主站知识库 `site-updates`，也不展示到首页最近更新。

## 2026-06-22

- 后台运行文案去除 slug 直出：
  - 第 11 轮 loop 将文章保存校验、视频分类列表摘要和分类完整提示里的 `slug` 直出改为“路径标识”。
  - 字段名和接口参数仍保持不变，只调整后台运行界面的中文可读性。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r9`；权限、保存逻辑和数据结构不变。

- 文章列表改用标题识别：
  - 第 10 轮 loop 将知识库文章列表主标题从路径标识改为文章标题，路径标识退到次级信息。
  - 后台文章列表接口只补充读取已有文章标题字段，不改变权限、保存逻辑或数据库结构。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r8`；后台文章管理接口仍要求 admin 权限。

- 顶部面板说明继续中文化：
  - 第 9 轮 loop 将后台切换面板后的顶部说明继续中文化，文章说明不再显示 `zh / en / ja`。
  - 视频说明减少平台名堆叠，访问来源说明补充“掩码 IP 前缀”，社交链接说明改为“社交入口”。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r7`；功能逻辑和接口不变。

- 未知页面路径显示收口：
  - 第 8 轮 loop 将页面显示兜底从原始路径改为“站内页面”，避免未登记页面在排行、洞察、账号活跃和点击事件中再次露出 `/xxx` 或语言参数。
  - 原始接口数据不改，仍可用于排查；本轮只调整后台展示层。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r6`；权限和数据写入不变。

- 统计覆盖文案校准：
  - 第 7 轮 loop 将侧边栏和实时大屏里的“已选站点 / 追踪项”改为“页面 / 地区 / 文章覆盖”。
  - 今日访问总览副标题也改为“多少个页面有访问”，避免把统计结果误读为后台配置数量。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r5`；统计接口、权限和数据写入不变。

- 实时大屏新增访问洞察摘要：
  - 第 6 轮 loop 在实时大屏顶部新增访问洞察摘要条，直接提炼最热页面、主要地区、热门文章和最高点击动作。
  - 摘要继续使用中文页面名、中文地区名和中文指标表达，减少在多张图表之间来回寻找结论的成本。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r4`；概览接口、后台权限、统计口径和主站公开更新边界不变。

- 热门文章改为表现比例条：
  - 第 5 轮 loop 将实时大屏的热门文章从表格改为文章表现比例条，和页面概览、地区概览、国家来源、点击热点保持一致的阅读方式。
  - 主数字展示浏览量，副信息展示访客、分类和最近访问时间，分类名尽量转为中文。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r3`；文章接口、后台权限、统计口径和主站公开更新边界不变。

- 访问来源与点击热点继续图表化：
  - 第 4 轮 loop 将“访问来源”页的国家来源从表格改为中文地区比例条，只显示关键前几名，减少滚动读表。
  - 将“点击埋点”页的点击热点从目标表格改为比例条，主数字改为点击数，副信息保留访客数、页面和最近时间。
  - 最近点击事件的目标位置改成更自然的中文说明，减少直接显示 `data_route`、`target_key` 这类技术字段。
  - 地区 / IP 来源明细表保留为工具表，方便继续查看和复制掩码 IP 前缀；后台权限、接口、统计口径和数据写入不变。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r2`；本次仍是后台私有 UI 更新，不写入主站 `site-updates` 或首页最近更新。

- 后台实时大屏图表化与侧边栏优化：
  - 将“实时城市分布、页面概览、地区概览”提前到后台实时大屏首屏，优先显示最需要判断的访问分布和页面表现。
  - 截图中原本很长的热门页面/国家表格改为比例条概览，只展示关键前几名，减少滚动阅读成本。
  - 页面路径统一转为中文显示：首页、视频区、知识库、资源区、游戏区、聊天室、关于我和已知知识库文章名，不再把 `/`、`/#videos`、`/?lang=zh` 当作主标题。
  - `TW`、`US`、`SG`、`CN` 等地区码在后台地图、概览、来源表和账号活跃记录中改为中文地区名。
  - 顶部横向小标签导航改为左侧栏，保留现有白底、细边框、蓝色数据强调的后台风格；后台权限、API、D1 schema 和主站公开更新边界不变。
  - 后台 CSS/JS query 更新为 `20260622-admin-insight-r1`；本次仍是后台私有 UI 更新，不写入主站 `site-updates` 或首页最近更新。

- 后台首屏中文化与信息口径优化：
  - 将实时面板标题、站点追踪摘要、访问排行和追踪项说明改为更直观的中文表达，减少模板感英文残留。
  - 将首屏表格标题和运行时统计摘要里的指标尽量改成“浏览 / 访客”，只在统计口径说明中保留一次缩写解释。
  - 将文章路径标识、三语编辑标签、分类路径标识和复制按钮改成更接近中文后台的说法，字段名、接口和保存逻辑不变。
  - 恢复实时大屏的浏览 / 访客统计口径说明，窄屏表格增加“左右滑动查看完整表格”提示，并补强焦点轮廓与状态文字换行。
  - 移除数据卡片里的 `Quota/token` 模板残留，改为真实统计口径说明，便于站长快速判断 PV、UV、点击、活跃访客和聊天消息含义。
  - 后台 CSS/JS query 更新为 `20260622-admin-usability-r2`；本次只调整后台私有 UI 文案和可读性，不写入主站 `site-updates` 或首页最近更新。

## 2026-06-21

- 参考图优先实时面板重做：
  - `/admin/` 在上一版极简后台基础上再次推倒重做，进一步贴近参考图的 Google Analytics 实时数据跟踪布局：顶部标题与按钮、跟踪网站横条、左侧实时总览与灰度地图、右侧网站实时排名、下方 property 卡片矩阵按例图重新组织。
  - 保留实时大屏、访问来源、点击埋点、知识库文章、视频管理、视频分类管理、聊天室管理、账号管理、社交链接、后台更新记录和后台说明全部模块，以及原导航顺序、表单字段、DOM 绑定 ID、权限校验、API 边界和 D1 schema。
  - 后台 CSS/JS query 更新为 `20260621-admin-ga-realtime-r2`；本次仍是后台私有视觉更新，不写入主站 `site-updates` 或首页最近更新。
- 极简数据工作台重做：
  - `/admin/` 整体改为接近 Google Analytics 实时面板的白底极简风格，使用大面积留白、细边框、轻阴影、黑色粗数字和少量蓝色数据强调。
  - 保留实时大屏、访问来源、点击埋点、知识库文章、视频管理、视频分类管理、聊天室管理、账号管理、社交链接、后台更新记录和后台说明全部模块及原导航顺序。
  - 保留所有后台 API、D1 schema、权限校验、表单字段、DOM 绑定 ID、视频安全解析、聊天室治理和账号安全边界；本轮只重做后台视觉与静态结构。
  - 实时大屏调整为趋势和榜单优先、灰度世界地图压轴的阅读顺序，更贴近参考图的数据工作台节奏。
  - 后台 CSS/JS query 更新为 `20260621-admin-ga-realtime-r2`；本次是后台私有视觉更新，不写入主站 `site-updates` 或首页最近更新。

## 2026-06-20

- 关于我社交链接管理：
  - 后台新增“社交链接”标签页，位置在“账号管理”和“后台更新记录”之间。
  - 可维护主站关于我窗口中的 X、GitHub、Bilibili、Instagram、Discord 五个跳转地址，并提供默认值恢复和当前链接预览。
  - 新增 `GET /api/admin/social-links`、`PUT /api/admin/social-links`，继续调用 `requireAdmin`；配置保存到 D1 `site_runtime_state.about_social_links`。
  - 公开主站通过 `GET /api/social-links` 只读读取配置；后台保存只接受 http(s) URL，省略协议时由服务端补 `https://`。
  - 后台 CSS/JS query 更新为 `20260620-admin-social-links-r1`；本次同时改变主站公开关于我窗口，因此公开侧另按主站规则写入 `site-updates`。

## 2026-06-19

- 访问地图投影对齐修复：
  - 修复宽屏下面板蓝色背景与真实世界地图 SVG 可见区域比例不一致时，点位按整块容器投影导致美国、中国、新加坡等来源落到大陆轮廓外的问题。
  - 来源点现在先计算本地世界地图 SVG 实际显示的 2:1 地图框，再把 Cloudflare 经纬度投影到该可见地图框内，保持点位和城市、国家位置一致。
  - 后台窗口尺寸变化、窄屏或重新切回实时大屏时会自动重算访问地图点位，避免面板尺寸变化后再次错位。
  - 后台 JS query 更新为 `20260619-admin-map-projection-r001`；后台私有更新不写入主站 `site-updates` 或首页最近更新。

- 访问地图真实底图与经纬度点位：
  - 实时大屏访问地图从手绘椭圆陆地改为本地真实世界地图轮廓资源，不依赖第三方在线地图或外部瓦片服务。
  - 来源点继续读取现有 Cloudflare 地理聚合字段，按经纬度投影到国家、地区和城市位置；缺少经纬度时才使用国家中心兜底。
  - 点位悬停、标题和辅助标签补充来源地区、PV/UV 与掩码 IP 前缀，继续不展示完整明文 IP。
  - 后台 CSS query 和 JS query 更新为 `20260619-admin-real-map-r001`；后台私有更新不写入主站 `site-updates` 或首页最近更新。

- 管理后台夜间 loop 可用性与权限加固：
  - 本轮按 2026-06-19 08:00（Asia/Shanghai）截止执行后台 loop，不新增 checkpoint 流水记录，只保留这一条后台私有合并记录。
  - 文章、视频、视频分类、聊天室、禁言和账号面板补强局部失败提示，视频/聊天并行读取改为局部失败可见，账号详情读取失败时保留账号列表并提示具体原因。
  - 后台面板切换同步 `hidden` / `aria-hidden`，文章三语编辑区补齐初始和运行时语义状态，侧边栏支持方向键、Home、End 导航。
  - 视频本地封面压缩和本地视频首帧截取期间锁定保存、发布、切换、清空和元数据按钮，文件选择器显示明确忙碌态。
  - `/admin/*` 中间件继续只允许 `users.role = admin`，并统一 `Cache-Control: no-store`、`X-Robots-Tag: noindex, nofollow`、`X-Content-Type-Options: nosniff` 和 `Referrer-Policy: same-origin`；D1/session 查询异常返回后台自己的 500 文本响应，畸形 session cookie 按未登录处理。
  - `scripts/build-check.mjs` 扩展后台结构、语义、权限、响应头、局部失败、公共 API 基础响应和主站边界检查；本轮未写入主站 `site-updates`、`js/main.js` fallback 或公开最近更新。
  - 后台 CSS query 为 `20260619-admin-loop-r001`，JS query 已更新为 `20260619-admin-loop-r014`。

## 2026-06-18

- 管理后台视觉改版循环更新：
  - 合并记录本线程今天的后台视觉改版循环，不为每个 checkpoint 单独新增后台页面内更新。
  - 优化后台登录/拒绝访问状态、侧边栏、顶部栏、内容区、状态条、表格横向滚动提示和移动端滚动边界。
  - 改善实时大屏、访问来源、点击埋点、文章管理、视频管理、视频分类、聊天室管理、账号管理、后台说明和后台更新记录的空态、按钮、复制、锁定、焦点、长文本和小屏展示。
  - 后台入口继续只允许 `users.role = admin` 的账号访问；后台私有更新仍只维护在 `adminUpdates` 和 `admin/docs/ADMIN_CHANGELOG.md`，不写入主站 `site-updates` 或首页最近更新。
  - 后台 CSS query 为 `20260618-admin-visual-r62`，JS query 已更新为 `20260618-admin-visual-r101`。

- 管理后台循环优化整合更新：
  - 将昨晚后台循环优化的多条页面内更新记录合并为一条后台私有更新说明，减少后台更新页被 checkpoint 流水账刷屏。
  - 本轮实际改动集中在后台渲染安全、账号与聊天室隐私保护、视频链接与 IP 前缀校验、表单写入/详情读取/列表刷新期间的锁定防护、重复请求和状态错位修复。
  - 同步优化视频分类占用提示、置顶排序联动、封面预览隐私、移动端输入提示、刷新状态和后台移动端可读性。
  - 后台私有更新仍只维护在 `adminUpdates` 和 `admin/docs/ADMIN_CHANGELOG.md`，不写入主站 `site-updates`、`js/main.js` 最近更新或公开知识库。
  - 后台 JS query 已更新为 `20260618-admin-loop-summary`。

## 2026-06-17

- 后台凭据表单语义优化：
  - 后台登录页和账号重置密码表单的邮箱输入保留 `type="email"` 格式校验，同时补齐浏览器标准 `autocomplete="username"`，减少浏览器对凭据表单的控制台提示。
  - 账号重置密码表单继续使用 `autocomplete="new-password"`，并保持不展示明文密码、不返回密码哈希的既有安全边界。
  - 站长登录流程、账号保存流程、`lusu_session` 校验、admin 权限复查和 `/api/admin/*` 权限边界保持不变。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台首屏按需加载优化：
  - 后台初始化改为只读取管理员身份和当前实时大屏数据，不再一次性拉取文章、视频、视频分类、聊天室、禁言和账号资料。
  - 切换到知识库文章、视频管理、视频分类管理、聊天室管理或账号管理时，再按需请求对应后台数据；手动刷新仍会强制刷新当前标签页。
  - 自动 30 秒刷新只保留在实时大屏、访问来源和点击埋点这组统计面板内，避免停留在账号或治理页面时后台反复拉取无关数据。
  - 概览接口失败时不再误标记为已加载，后续切换或手动刷新仍可重新请求。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

## 2026-06-16

- 视频分类 seed 持久化修复：
  - 运行时 schema guard 只在全新 `video_categories` 表首次创建时初始化默认视频分类。
  - 已有视频分类表会写入 `site_runtime_state` 的 `video_categories_default_seeded` 标记，之后后台删除默认标签、停用标签或调整排序都不会被冷启动自动补回。
  - `cloudflare/schema.sql` 同步增加状态表和条件 seed，减少手动 migration 复原后台维护结果的风险；旧库缺 `pinned_sort_order` 时，队列索引继续交给运行时 guard 补列后创建。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 视频置顶独立队列排序修复：
  - D1 `videos` 新增 `pinned_sort_order`，置顶视频走独立队列；只要勾选置顶，就一定排在未置顶视频前面。
  - 公开视频列表和后台视频列表统一按置顶优先、置顶排序从大到小、普通排序从大到小返回。
  - 后台视频表单新增“置顶排序”，列表徽章显示置顶排序值；勾选置顶时如果置顶排序为空或为 0，会自动填入当前置顶队列的下一个排序值。
  - 旧 D1 库首次补列时会把已置顶视频的普通排序回填为置顶排序，避免上线后已有置顶视频顺序突变。

- 视频封面本地上传与首帧生成：
  - 后台视频管理新增本地封面选择，支持 JPG、PNG、WEBP、AVIF 图片输入，并在浏览器端压缩裁切为 16:9 封面数据后填入现有 `thumbnail_url`。
  - 新增“从本地视频截首帧”控件；如果封面为空且已选择本地视频，保存前会自动读取本机视频第一帧生成封面。
  - 后端 `thumbnail_url` 校验保留 YouTube / Bilibili 图片域名白名单，同时允许受限 `data:image` 封面并限制大小，不允许 SVG/HTML 伪装图片。
  - 同步更新后台页面 `adminUpdates`、后台资源 query、项目变更记录和视频维护规则。

- 视频分类与 Bilibili 元数据维护修复：
  - 默认视频分类 seed 改为只插入缺失分类，不再覆盖后台维护过的 slug、中文名、英文名、日文名、排序和启用状态，避免冷启动或 schema guard 后把分类名还原。
  - 后台保存已有视频时，如果视频 URL 没有变化，不再重复抓取 YouTube / Bilibili 外部元数据，减少 Bilibili 风控或 HTTP 412 对普通编辑保存的影响。
  - Bilibili 元数据抓取补强请求头、详情接口、移动页、`__NEXT_DATA__`、页面标题和 meta 兜底；抓取失败时保留可读错误，并提示管理员手动补全标题、作者和封面。
  - 后台新增重复视频拦截，避免同一平台同一视频 ID 被重复录入。
  - 视频分类勾选区标识停用分类，新视频不能继续选择未关联的停用分类；已关联的停用分类仍保留显示，方便管理员移除。
  - 同步更新 `adminUpdates`、后台 CSS/JS query、后台维护规则和项目变更记录。

## 2026-06-15

- 账号管理和统计口径优化：
  - 后台新增“账号管理”标签页，位置在“后台更新记录”上方。
  - 账号页可查看所有注册账号的邮箱、角色、密码加密状态、最近登录、活跃会话、云存档数量、登录履历和近期站内活跃。
  - 密码不展示明文，也不把 `password_hash` 返回给前端；后台只提供设置新密码的能力，保存后会清空输入框。
  - 新增 `user_login_events` 表记录成功登录/注册后的登录履历，只保存掩码 IP 前缀、IP hash、地区、设备摘要和时间，不保存完整明文 IP。
  - 访问统计改为登录账号优先识别：已登录用户按账号合并为同一个不可逆统计 ID，同一账号多设备、多次访问仍只计为 1 个 UV；匿名访客继续使用隐藏访客 cookie。
  - 实时大屏新增自然语言说明，解释 PV、UV、在线访客和点击数据的含义，减少只看缩写造成的误读。
- 视频排序和 Bilibili 元数据兜底修复：
  - 后台视频列表和公开视频列表统一为置顶优先，未置顶视频按排序值从大到小显示。
  - 后台新建视频默认填入当前最大排序 +10，视频分类新建也使用同样的默认追加逻辑。
  - 默认视频分类 seed 不再覆盖后台维护过的排序和启用状态，避免冷启动后分类排序被还原。
  - Bilibili 元数据抓取在 API 412 后继续尝试页面 meta、结构化数据和更宽的页面状态解析，减少标题、作者、发布时间、简介和封面缺失。
  - 后台视频列表增加排序值提示，便于确认前台展示顺序。

- 视频元数据和后台更新记录优化：
  - 后台视频预览改为限制宽度的小播放器，避免 YouTube / Bilibili iframe 在编辑区占满整页。
  - YouTube 元数据抓取在 oEmbed 基础上补充页面元信息解析，尽量自动填入简介和发布时间。
  - Bilibili 元数据抓取增加浏览器化请求头、页面 `__INITIAL_STATE__` 备用解析和 b23 短链跟随兜底，降低 HTTP 412 或跳转异常导致的解析失败。
  - 外部元数据简介改为安全截断，避免长简介导致整次抓取失败。
  - 新增独立“后台更新记录”标签页，后台导航顺序调整为实时大屏、访问来源、点击埋点、知识库文章、视频管理、视频分类管理、聊天室管理、后台更新记录、后台说明。
  - `admin/docs/ADMIN_SKILL.md` 新增硬性规则：每次后台更新后必须同步维护后台页面内 `adminUpdates` 和本文档。

- 新增后台专用文档包：
  - 新增 `admin/docs/ADMIN_PROJECT_CONTEXT.md`，作为 `/admin/` 管理后台专用上下文。
  - 新增 `admin/docs/ADMIN_SKILL.md`，沉淀后台权限、隐私、视频、文章、聊天室和缓存维护规则。
  - 新增 `admin/docs/ADMIN_CHANGELOG.md`，将后台私有更新记录从主站公开更新体系中进一步区分出来。
  - 明确后台专用文档不等同于主站 `PROJECT_CONTEXT.md`、根目录 `CHANGELOG.md` 或主站项目 Skill。

- 视频管理系统：
  - 后台新增“视频管理”和“视频分类管理”模块。
  - 支持输入 YouTube / Bilibili / b23.tv 链接，由服务端识别平台、生成规范化 `embed_url` 并抓取标题、简介、作者和封面。
  - 支持视频草稿、发布、隐藏、排序、置顶、删除、刷新元数据和分类关联。
  - 支持视频分类新增、编辑、停用、排序和删除；“全部”分类继续只由前台生成，不写入数据库。

- 后台视觉优化第一版：
  - 统一侧边栏、顶部栏、XP 面板、按钮、表格、状态标签、空状态和提示信息。
  - 改善实时大屏、文章管理、访问来源、点击埋点和聊天室管理在桌面、平板、移动端的阅读布局。
  - 修正移动端侧边栏高度和编辑区滚动边界。
  - 后台更新记录继续独立于主站知识库 `site-updates`。

- 文章访问 PV/UV 统计：
  - 公开文章详情接口成功返回文章时写入 `article_view_events`。
  - 后台实时大屏新增热门文章表，展示文章标题、slug、PV、UV 和最近访问时间。
  - 后台文章列表和文章编辑详情新增每篇文章总 PV/UV 与今日 PV/UV。

- 管理后台 MVP 接入：
  - 新增独立 `/admin/` 中文管理后台。
  - 新增实时监控大屏、知识库文章管理、访问来源、点击埋点、聊天室管理、后台项目介绍和私有后台更新记录。
  - 新增 `functions/admin/_middleware.js`，后台静态资源复用主站 `lusu_session` 并校验 `users.role = admin`。
  - 所有 `/api/admin/*` 接口继续使用服务端 admin 权限校验。
  - 聊天室后台支持消息编辑、隐藏/恢复、删除，并按隐藏 visitor id 或 IP hash 禁言。
