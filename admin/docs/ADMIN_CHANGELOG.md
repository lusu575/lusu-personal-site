# 鲁肃个人站管理后台专用 CHANGELOG

> 管理后台专用说明：本文档只记录 `/admin/` 管理后台的私有维护记录，不等同于主站根目录 `CHANGELOG.md`。后台私有更新不写入主站知识库 `site-updates`，也不展示到首页最近更新。

## 2026-06-18

- 账号详情列表渲染安全优化：
  - 账号管理的登录履历、近期活跃和会话状态改用 `document.createElement` 与 `textContent` 渲染事件标题、来源位置、路径、IP 掩码、设备摘要和时间。
  - 现有三块详情面板、空状态和事件条目样式保持不变，但不再通过 HTML 字符串拼接登录履历、近期活跃或会话字段。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 账号列表渲染安全优化：
  - 账号管理列表改用 `document.createElement`、`textContent`、`classList` 和 `dataset` 渲染邮箱、角色、密码状态、活跃会话和登录摘要。
  - 现有列表选中态、状态徽标、空状态和点击读取详情的行为保持不变，但不再通过 HTML 字符串拼接账号字段。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 聊天室消息列表渲染安全优化：
  - 聊天室管理的消息列表改用 `document.createElement`、`textContent`、`classList` 和 `dataset` 渲染昵称、可见状态、来源、消息摘要和发送时间。
  - 现有列表选中态、隐藏/可见徽标、来源徽标、空状态和点击进入编辑的行为保持不变，但不再通过 HTML 字符串拼接用户昵称或消息内容。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 聊天室禁言列表渲染安全优化：
  - 聊天室管理的禁言记录列表改用 `document.createElement`、`textContent` 和 `dataset` 渲染状态、类型、禁言对象、原因、期限和停用按钮。
  - 现有禁言列表样式、停用按钮和刷新 / 停用忙碌态保持不变，但不再通过 HTML 字符串拼接禁言审计字段。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 聊天室元信息渲染安全优化：
  - 聊天室管理选中消息后，隐藏用户 ID、前端 client id、IP hash、IP 前缀和来源信息改用 `document.createElement` 与 `textContent` 渲染。
  - 现有展示字段、顺序和中文标签保持不变，但不再通过 HTML 字符串拼接这些审计字段。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台更新记录渲染安全优化：
  - 后台“后台更新记录”标签页改用 `document.createElement` 和 `textContent` 渲染日期、标题和正文，减少后续维护时误把更新文案当作 HTML 执行的风险。
  - 现有后台私有更新内容、排序和视觉样式保持不变。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台表单状态换行稳定性优化：
  - 文章、视频、视频分类和账号表单的状态提示增加弹性宽度、最小宽度、行高和任意位置换行，长错误文案不再挤压操作按钮。
  - 520px 以下表单状态提示改为独占整行，和按钮保持现有移动端纵向排列。
  - 后台页面内 `adminUpdates`、后台 CSS query 和后台 JS query 已同步更新。

- 后台表单状态可访问性优化：
  - 知识库文章、视频管理、视频分类管理和账号管理的状态提示补充 `role="status"`、礼貌播报和原子更新语义。
  - 保存、删除、加载失败等状态文字仍保持原中文文案和现有位置，但更容易被辅助技术感知。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 聊天室禁言列表忙碌态优化：
  - 禁言列表手动刷新时会显示“刷新中...”并临时禁用禁言列表操作，避免重复读取。
  - 停用禁言时对应按钮会显示“停用中...”，同时临时禁用其它停用按钮和刷新按钮，减少重复停用请求。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 聊天室禁言按钮忙碌态优化：
  - 聊天室管理按隐藏用户 ID 或 IP 来源禁言时，会临时禁用保存、隐藏/恢复、删除和禁言按钮，避免慢网络下重复提交禁言请求。
  - 对应禁言按钮会显示“禁言中...”；失败时继续在选中记录标题处显示错误。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 聊天室治理按钮忙碌态优化：
  - 聊天室管理保存修改、隐藏/恢复或删除消息时，会临时禁用保存、隐藏/恢复、删除和禁言按钮，避免慢网络下重复提交同一条聊天治理操作。
  - 对应按钮会显示“保存中...”“隐藏中...”“恢复中...”或“删除中...”；失败时会在选中记录标题处显示错误。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 视频删除防重复操作优化：
  - 视频管理执行“删除”时，会临时禁用保存、发布和删除按钮，避免慢网络下重复删除同一个视频。
  - 删除请求进行中会显示“删除中...”，完成后在新建视频表单状态区保留“已删除。”提示。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 文章删除防重复操作优化：
  - 知识库文章执行“删除”时，会临时禁用保存、发布和删除按钮，避免慢网络下重复删除同一篇文章。
  - 删除请求进行中会显示“删除中...”，完成后在新建表单状态区保留“已删除。”提示。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 视频分类保存删除防重复操作优化：
  - 视频分类管理执行“保存分类”时，会临时禁用保存和删除按钮，避免慢网络下重复提交分类新增或编辑。
  - 删除分类请求进行中时会显示“删除中...”，并临时禁用保存和删除按钮，避免重复删除同一分类。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 文章保存防重复提交优化：
  - 知识库文章执行“保存”或“保存并发布”时，会临时禁用保存、发布和删除按钮，避免慢网络下重复提交文章写入。
  - 草稿保存会显示“保存中...”，发布保存会显示“发布中...”，请求结束后恢复原按钮文案和可用状态，并保留保存成功提示。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 视频保存防重复提交优化：
  - 视频管理执行“保存”或“保存并发布”时，会临时禁用保存、发布和删除按钮，避免慢网络下重复提交视频写入。
  - 草稿保存会显示“保存中...”，发布保存会显示“发布中...”，请求结束后恢复原按钮文案和可用状态，并保留保存成功提示。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 视频元数据按钮忙碌态优化：
  - 视频管理执行“自动识别/获取信息”时，识别按钮会显示“识别中...”并临时禁用识别和刷新按钮，避免重复提交外部元数据抓取。
  - 刷新已保存视频元数据时，刷新按钮会显示“刷新中...”；新建视频未保存前继续禁用“刷新元数据”。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 账号保存防重复提交优化：
  - 账号管理未选择账号时会禁用“保存账号”，避免空表单提交。
  - 保存账号或重置密码请求进行中时，按钮会显示“保存中...”并临时禁用，减少慢网络下重复提交角色修改或密码重置。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 聊天室治理操作状态优化：
  - 聊天室管理未选择记录时会禁用“保存修改”“隐藏消息”“删除”“禁言用户ID”和“禁言IP来源”，避免空操作和误点。
  - 选择聊天记录后按钮恢复可用；隐藏/恢复按钮会按当前记录状态显示“隐藏消息”或“恢复消息”。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台顶部状态文本换行优化：
  - 顶部刷新状态提示增加最大宽度、换行和容器收缩约束，长错误文案在窄屏或中等宽度窗口下不再挤压“刷新”“退出”按钮。
  - 760px 以下顶部操作区占满可用宽度，保持状态、刷新和退出操作按现有移动端规则自然换行。
  - 后台页面内 `adminUpdates`、后台 CSS query 和后台 JS query 已同步更新。

- 后台退出中手动刷新防护：
  - 后台退出流程开始后会同步禁用顶部“刷新”按钮，并显示“暂停刷新”，避免退出挂起时继续手动触发后台统计请求。
  - `loadPanelData` 在退出中会直接返回，作为按钮禁用之外的第二层前端防护；退出失败后刷新按钮会恢复原状态，错误提示会短暂保留，避免被刚完成的概览刷新覆盖。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台退出中暂停自动刷新：
  - 后台退出流程开始后会暂停 30 秒自动刷新，避免退出请求挂起时继续发送统计面板请求。
  - 如果退出请求失败，按钮和顶部错误状态会恢复，同时自动刷新定时器也会恢复，保留后续实时大屏更新能力。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台导航当前项语义优化：
  - 侧边栏当前标签会同步 `aria-current="page"`，切换标签时自动更新，键盘浏览和辅助技术能更清楚地识别当前所在模块。
  - 该变化只影响导航语义，不改变后台模块顺序、权限、数据读取或视觉布局。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台退出按钮防连点优化：
  - 点击“退出”后按钮会进入“退出中...”状态并临时禁用，避免慢网络或连续点击时重复提交登出请求。
  - 如果退出请求失败，按钮会恢复为“退出”，并在顶部状态区显示错误，方便继续重试。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台刷新状态可访问性优化：
  - 顶部刷新状态改为 `role="status"` 的礼貌 live region，数据读取、刷新完成和错误提示更容易被辅助技术感知。
  - “刷新”按钮会根据当前标签、读取中和无需刷新三种状态同步 `aria-label` 与悬停提示，减少只读按钮文字造成的语义不清。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台标签页记忆优化：
  - 后台会在当前浏览器会话中记住最后打开的标签页，刷新页面后回到上次工作位置，减少维护文章、视频、账号时的重复跳转。
  - 记忆值只保存在本地 `sessionStorage`，不会写入 D1 或后台接口；旧值或异常值会自动回到实时大屏。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台静态面板刷新状态优化：
  - 后台更新记录和后台说明属于页面内本地静态内容，进入后顶部刷新按钮会显示“无需刷新”并禁用，避免误以为会重新请求后台接口。
  - 程序化刷新静态面板时只更新状态提示，不触发额外 `/api/admin/*` 请求。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台自动刷新可见性优化：
  - 实时大屏、访问来源和点击埋点的 30 秒自动刷新在页面隐藏时暂停，减少后台标签页长期打开时的无效请求。
  - 页面回到前台且仍停留在统计面板时，会自动补一次刷新，保持实时数据可用。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台刷新忙碌状态优化：
  - 后台面板数据读取中会同步禁用顶部“刷新”按钮，并显示“刷新中...”，减少快速连点造成的误解。
  - 请求结束或失败后按钮会自动恢复，仍可继续手动刷新重试。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台面板请求合并优化：
  - 后台同一标签页数据正在读取时，快速切换或连续点击刷新会复用当前请求，避免并发打出重复 `/api/admin/*` 请求。
  - 请求成功后继续标记当前面板已加载；请求失败后会释放读取状态，后续切换或手动刷新仍可重试。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

## 2026-06-17

- 后台凭据表单语义优化：
  - 后台登录页和账号重置密码表单的邮箱输入保留 `type="email"` 格式校验，同时补齐浏览器标准 `autocomplete="username"`，减少浏览器对凭据表单的控制台提示。
  - 账号重置密码表单继续使用 `autocomplete="new-password"`，并保持不展示明文密码、不返回密码哈希的既有安全边界。
  - 站长登录流程、账号保存流程、`lusu_session` 校验、admin 权限复查和 `/api/admin/*` 权限边界保持不变。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 后台首屏按需加载优化：
  - 后台初始化改为只读取管理员身份和当前实时大屏数据，不再一次性拉取文章、视频、视频分类、聊天室、禁言和账号资料。
  - 切换到知识库文章、视频管理、视频分类管理、聊天室管理或账号管理时，再按需请求对应后台数据；手动刷新仍会强制刷新当前标签页。
  - 自动 30 秒刷新只保留在实时大屏、访问来源和点击埋点这组统计面板内，避免停留在账号或治理页面时后台反复拉取无关数据。
  - 概览接口失败时不再误标记为已加载，后续切换或手动刷新仍可重新请求。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

## 2026-06-16

- 视频分类 seed 持久化修复：
  - 运行时 schema guard 只在全新 `video_categories` 表首次创建时初始化默认视频分类。
  - 已有视频分类表会写入 `site_runtime_state` 的 `video_categories_default_seeded` 标记，之后后台删除默认标签、停用标签或调整排序都不会被冷启动自动补回。
  - `cloudflare/schema.sql` 同步增加状态表和条件 seed，减少手动 migration 复原后台维护结果的风险；旧库缺 `pinned_sort_order` 时，队列索引继续交给运行时 guard 补列后创建。
  - 后台页面内 `adminUpdates` 和后台 JS query 已同步更新。

- 视频置顶独立队列排序修复：
  - D1 `videos` 新增 `pinned_sort_order`，置顶视频走独立队列；只要勾选置顶，就一定排在未置顶视频前面。
  - 公开视频列表和后台视频列表统一按置顶优先、置顶排序从大到小、普通排序从大到小返回。
  - 后台视频表单新增“置顶排序”，列表徽章显示置顶排序值；勾选置顶时如果置顶排序为空或为 0，会自动填入当前置顶队列的下一个排序值。
  - 旧 D1 库首次补列时会把已置顶视频的普通排序回填为置顶排序，避免上线后已有置顶视频顺序突变。

- 视频封面本地上传与首帧生成：
  - 后台视频管理新增本地封面选择，支持 JPG、PNG、WEBP、AVIF 图片输入，并在浏览器端压缩裁切为 16:9 封面数据后填入现有 `thumbnail_url`。
  - 新增“从本地视频截首帧”控件；如果封面为空且已选择本地视频，保存前会自动读取本机视频第一帧生成封面。
  - 后端 `thumbnail_url` 校验保留 YouTube / Bilibili 图片域名白名单，同时允许受限 `data:image` 封面并限制大小，不允许 SVG/HTML 伪装图片。
  - 同步更新后台页面 `adminUpdates`、后台资源 query、项目变更记录和视频维护规则。

- 视频分类与 Bilibili 元数据维护修复：
  - 默认视频分类 seed 改为只插入缺失分类，不再覆盖后台维护过的 slug、中文名、英文名、日文名、排序和启用状态，避免冷启动或 schema guard 后把分类名还原。
  - 后台保存已有视频时，如果视频 URL 没有变化，不再重复抓取 YouTube / Bilibili 外部元数据，减少 Bilibili 风控或 HTTP 412 对普通编辑保存的影响。
  - Bilibili 元数据抓取补强请求头、详情接口、移动页、`__NEXT_DATA__`、页面标题和 meta 兜底；抓取失败时保留可读错误，并提示管理员手动补全标题、作者和封面。
  - 后台新增重复视频拦截，避免同一平台同一视频 ID 被重复录入。
  - 视频分类勾选区标识停用分类，新视频不能继续选择未关联的停用分类；已关联的停用分类仍保留显示，方便管理员移除。
  - 同步更新 `adminUpdates`、后台 CSS/JS query、后台维护规则和项目变更记录。

## 2026-06-15

- 账号管理和统计口径优化：
  - 后台新增“账号管理”标签页，位置在“后台更新记录”上方。
  - 账号页可查看所有注册账号的邮箱、角色、密码加密状态、最近登录、活跃会话、云存档数量、登录履历和近期站内活跃。
  - 密码不展示明文，也不把 `password_hash` 返回给前端；后台只提供设置新密码的能力，保存后会清空输入框。
  - 新增 `user_login_events` 表记录成功登录/注册后的登录履历，只保存掩码 IP 前缀、IP hash、地区、设备摘要和时间，不保存完整明文 IP。
  - 访问统计改为登录账号优先识别：已登录用户按账号合并为同一个不可逆统计 ID，同一账号多设备、多次访问仍只计为 1 个 UV；匿名访客继续使用隐藏访客 cookie。
  - 实时大屏新增自然语言说明，解释 PV、UV、在线访客和点击数据的含义，减少只看缩写造成的误读。
- 视频排序和 Bilibili 元数据兜底修复：
  - 后台视频列表和公开视频列表统一为置顶优先，未置顶视频按排序值从大到小显示。
  - 后台新建视频默认填入当前最大排序 +10，视频分类新建也使用同样的默认追加逻辑。
  - 默认视频分类 seed 不再覆盖后台维护过的排序和启用状态，避免冷启动后分类排序被还原。
  - Bilibili 元数据抓取在 API 412 后继续尝试页面 meta、结构化数据和更宽的页面状态解析，减少标题、作者、发布时间、简介和封面缺失。
  - 后台视频列表增加排序值提示，便于确认前台展示顺序。

- 视频元数据和后台更新记录优化：
  - 后台视频预览改为限制宽度的小播放器，避免 YouTube / Bilibili iframe 在编辑区占满整页。
  - YouTube 元数据抓取在 oEmbed 基础上补充页面元信息解析，尽量自动填入简介和发布时间。
  - Bilibili 元数据抓取增加浏览器化请求头、页面 `__INITIAL_STATE__` 备用解析和 b23 短链跟随兜底，降低 HTTP 412 或跳转异常导致的解析失败。
  - 外部元数据简介改为安全截断，避免长简介导致整次抓取失败。
  - 新增独立“后台更新记录”标签页，后台导航顺序调整为实时大屏、访问来源、点击埋点、知识库文章、视频管理、视频分类管理、聊天室管理、后台更新记录、后台说明。
  - `admin/docs/ADMIN_SKILL.md` 新增硬性规则：每次后台更新后必须同步维护后台页面内 `adminUpdates` 和本文档。

- 新增后台专用文档包：
  - 新增 `admin/docs/ADMIN_PROJECT_CONTEXT.md`，作为 `/admin/` 管理后台专用上下文。
  - 新增 `admin/docs/ADMIN_SKILL.md`，沉淀后台权限、隐私、视频、文章、聊天室和缓存维护规则。
  - 新增 `admin/docs/ADMIN_CHANGELOG.md`，将后台私有更新记录从主站公开更新体系中进一步区分出来。
  - 明确后台专用文档不等同于主站 `PROJECT_CONTEXT.md`、根目录 `CHANGELOG.md` 或主站项目 Skill。

- 视频管理系统：
  - 后台新增“视频管理”和“视频分类管理”模块。
  - 支持输入 YouTube / Bilibili / b23.tv 链接，由服务端识别平台、生成规范化 `embed_url` 并抓取标题、简介、作者和封面。
  - 支持视频草稿、发布、隐藏、排序、置顶、删除、刷新元数据和分类关联。
  - 支持视频分类新增、编辑、停用、排序和删除；“全部”分类继续只由前台生成，不写入数据库。

- 后台视觉优化第一版：
  - 统一侧边栏、顶部栏、XP 面板、按钮、表格、状态标签、空状态和提示信息。
  - 改善实时大屏、文章管理、访问来源、点击埋点和聊天室管理在桌面、平板、移动端的阅读布局。
  - 修正移动端侧边栏高度和编辑区滚动边界。
  - 后台更新记录继续独立于主站知识库 `site-updates`。

- 文章访问 PV/UV 统计：
  - 公开文章详情接口成功返回文章时写入 `article_view_events`。
  - 后台实时大屏新增热门文章表，展示文章标题、slug、PV、UV 和最近访问时间。
  - 后台文章列表和文章编辑详情新增每篇文章总 PV/UV 与今日 PV/UV。

- 管理后台 MVP 接入：
  - 新增独立 `/admin/` 中文管理后台。
  - 新增实时监控大屏、知识库文章管理、访问来源、点击埋点、聊天室管理、后台项目介绍和私有后台更新记录。
  - 新增 `functions/admin/_middleware.js`，后台静态资源复用主站 `lusu_session` 并校验 `users.role = admin`。
  - 所有 `/api/admin/*` 接口继续使用服务端 admin 权限校验。
  - 聊天室后台支持消息编辑、隐藏/恢复、删除，并按隐藏 visitor id 或 IP hash 禁言。
